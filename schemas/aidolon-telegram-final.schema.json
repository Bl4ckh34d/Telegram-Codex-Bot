{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AIDOLON Telegram final response",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "text": {
      "type": "string",
      "description": "The concise Telegram-ready final answer. Use English, German, or Chinese only."
    },
    "spoken": {
      "type": "string",
      "description": "Optional voice-ready answer for voice replies."
    },
    "text_only": {
      "type": "string",
      "description": "Optional non-spoken details, commands, links, or attachment notes."
    },
    "attachments": {
      "type": "array",
      "description": "Files saved under the bot attachment output folder.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "path": {
            "type": "string",
            "description": "Path relative to runtime/out, without a runtime/out prefix."
          },
          "caption": {
            "type": "string"
          }
        },
        "required": ["path"]
      }
    },
    "status": {
      "type": "string",
      "enum": ["ok", "blocked", "failed"]
    }
  },
  "required": ["text"]
}
